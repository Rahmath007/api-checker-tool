# API Checker Tool

A simple Python script that checks if an API endpoint is working.

## Features

- Asks for an API URL
- Sends a GET request using `requests`
- Prints the HTTP status code
- Handles timeouts and connection errors

## How to Run

```bash
python api_checker.py
```
